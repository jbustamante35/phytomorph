%% first generation script to take images is: takeImages.sh
    %% this shell script runs the quickCheck compiled version at MN and UW

%% second generation is an attempt to make the streaming of files better

